import { PhonetransformPipe } from './phonetransform.pipe';

describe('PhonetransformPipe', () => {
  it('create an instance', () => {
    const pipe = new PhonetransformPipe();
    expect(pipe).toBeTruthy();
  });
});
