import { CustomeTimeAgoPipe } from './custome-time-ago.pipe';

describe('CustomeTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
